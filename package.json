{
  "name": "core-finance",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "dotnet run",
    "build": "cd ClientApp && npm run build && cd ../ && dotnet build",
    "test": "dotnet test CoreFinance.csproj",
    "clean": "dotnet clean"
  }
}